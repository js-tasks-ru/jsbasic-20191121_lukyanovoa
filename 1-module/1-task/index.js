'use strict';
/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */
let x = +prompt('Введите x', ''); 
let n = +prompt('Введите n', ''); 
function pow (x, n) {
  return x ** n;
}

